# Copy this file to .env and configure the values in Coolify environment variables.
# Do not hardcode real secrets in docker-compose.yaml.

DEFAULT_USER=
DEFAULT_WORKSPACE=
ALLOW_WRITE=true
DOMAIN=agentdesk.example.com
HTTP_USERNAME=admin
HTTP_PASSWORD=change-me
BRIDGE_ENDPOINT=host.docker.internal:2222
